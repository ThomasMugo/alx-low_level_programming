#include <stdio.h>

/**
 * main - prints program name
 * @argc: number of arguments
 * @argv: argument array
 * Return: 0 if successful
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
